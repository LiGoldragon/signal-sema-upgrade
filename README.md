# signal-sema-upgrade

`signal-sema-upgrade` is the ordinary working signal contract for the
`sema-upgrade` component.

The first prototype is intentionally narrow: it names component schema
versions, lists migrations compiled into the current daemon build, and
attempts the `persona-spirit` `0.1.0` to `0.1.1` upgrade path.
